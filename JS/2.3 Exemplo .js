var contador = 0;
while(contador <= 10){
    //Para cada valor  do contador o loop for rada 10 vezes.
    //Contador vezes o i gera os multiplos da tabuada começado com a tabuada do 0 e terminado com a tabuada do 10.
    for (let i = 0; i <= 10; i++) {
        console.log(`contador = ${contador} e i = ${i}: contador*i = ${contador*i}`)
        
    }
    contador++
}
